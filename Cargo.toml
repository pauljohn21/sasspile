[package]
name = "sasspile-rx"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"

[dependencies]
rxrust = { path = "../rx" }
tracing = "0.1"
tracing-subscriber = "0.3"

[patch.crates-io]
rxrust = { path = "../rx" }

[lints.rust]
unsafe_code = "warn"

[lints.clippy]
all = "warn"
pedantic = "warn"
