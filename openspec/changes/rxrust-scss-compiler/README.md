# rxrust-scss-compiler

Build SCSS compiler with rxrust philosophy: sass-spec as behavior spec, rxrust observables as implementation. input = Observable stream, output = Result. No dart-sass reference.
