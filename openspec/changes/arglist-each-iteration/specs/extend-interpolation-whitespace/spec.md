# spec: @extend 选择器中插值空格误判

## 概述

`@extend #{'%shared-' + $name}` 在选择器判断前收到未求值字符串，
`+` 周围空格被误判为复杂多-compound 选择器。

## 行为规则

- 选择器中的 `#{...}` 插值块内部空格不触发复杂选择器错误
- 插值块外部的空格仍按原规则判断（含空格 → 报错）
- 多个插值块时，所有 `#{...}` 都跳过
